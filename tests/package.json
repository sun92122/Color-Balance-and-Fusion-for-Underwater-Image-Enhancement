{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxt generate && echo > .output/public/.nojekyll && gh-pages -d .output/public --dotfiles"
  },
  "dependencies": {
    "@primeuix/themes": "^1.1.1",
    "nuxt": "^3.17.4",
    "primeicons": "^7.0.0",
    "primevue": "^4.3.5",
    "vue": "^3.5.16",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@primevue/nuxt-module": "^4.3.5",
    "gh-pages": "^6.3.0"
  }
}
